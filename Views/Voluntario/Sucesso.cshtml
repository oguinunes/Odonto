@{
    ViewData["Title"] = "Solicitação Enviada";
}

<style>
    :root {
        --cor-primaria: #2c5282;
        --cor-esperanca: #48bb78;
    }

    .sucesso-container {
        max-width: 700px;
        margin: 80px auto;
        text-align: center;
        padding: 20px;
    }

    .sucesso-card {
        background: white;
        padding: 60px 40px;
        border-radius: 20px;
        box-shadow: 0 10px 40px rgba(0,0,0,0.1);
    }

    .sucesso-icon {
        font-size: 5rem;
        color: var(--cor-esperanca);
        margin-bottom: 30px;
        animation: scaleIn 0.5s ease-out;
    }

    @@keyframes scaleIn {
        0%

    {
        transform: scale(0);
    }

    50% {
        transform: scale(1.1);
    }

    100% {
        transform: scale(1);
    }

    }

    .sucesso-title {
        color: var(--cor-primaria);
        font-size: 2.5rem;
        font-weight: bold;
        margin-bottom: 20px;
    }

    .sucesso-message {
        font-size: 1.2rem;
        color: #4a5568;
        margin-bottom: 40px;
        line-height: 1.8;
    }

    .btn-home {
        background: linear-gradient(135deg, var(--cor-primaria), #4a90e2);
        color: white;
        padding: 15px 40px;
        border-radius: 10px;
        text-decoration: none;
        font-size: 1.1rem;
        font-weight: bold;
        display: inline-block;
        transition: all 0.3s;
    }

        .btn-home:hover {
            transform: translateY(-3px);
            box-shadow: 0 5px 20px rgba(44, 82, 130, 0.4);
            color: white;
        }

    .info-box {
        background: #f0f9ff;
        border-left: 4px solid var(--cor-esperanca);
        padding: 20px;
        margin-top: 40px;
        border-radius: 8px;
        text-align: left;
    }

        .info-box h4 {
            color: var(--cor-primaria);
            margin-top: 0;
            margin-bottom: 15px;
        }

        .info-box p {
            margin-bottom: 10px;
            color: #2d3748;
        }
</style>

<div class="sucesso-container">
    <div class="sucesso-card">
        <div class="sucesso-icon">
            <i class="fas fa-check-circle"></i>
        </div>

        <h1 class="sucesso-title">Solicitação Enviada!</h1>

        <p class="sucesso-message">
            Sua solicitação para ser voluntário foi recebida com sucesso! ❤️<br>
            Nossa equipe analisará seus dados e entrará em contato em breve.
        </p>

        <div class="info-box">
            <h4><i class="fas fa-info-circle"></i> Próximos Passos</h4>
            <p>📧 Você receberá um email de confirmação</p>
            <p>👨‍⚕️ Nossa equipe analisará sua solicitação</p>
            <p>📞 Entraremos em contato por email ou telefone</p>
            <p>💙 Juntos faremos a diferença na comunidade!</p>
        </div>

        <a href="/" class="btn-home">
            <i class="fas fa-home"></i> Voltar para Home
        </a>
    </div>
</div>