@model Pi_Odonto.Models.Responsavel
@{
    ViewData["Title"] = "Minha Família - Casa Espírita Trabalhadores de Jesus";
}

<style>
    :root {
        --cor-primaria: #4A90E2;
        --cor-secundaria: #66BB6A;
        --cor-accent: #FFD700;
        --cor-info: #17a2b8;
        --cor-amor: #ec2651ff; /* Cor usada para ações de amor/cuidado e, neste caso, para o alerta de saída */
    }

    body {
        font-family: 'Poppins', sans-serif;
        background: linear-gradient(135deg, #f8f9fa 0%, #e9ecef 100%);
        min-height: 100vh;
    }

    .container-compacto {
        max-width: 1000px;
        margin: 1rem auto;
        padding: 0 1rem;
    }

    .header-compacto {
        background: linear-gradient(135deg, var(--cor-primaria), var(--cor-secundaria));
        color: white;
        padding: 1rem;
        margin-bottom: 1rem;
        border-radius: 15px;
        text-align: center;
    }

    .header-title {
        font-size: 1.3rem;
        font-weight: 600;
        margin-bottom: 0.25rem;
    }

    .header-subtitle {
        font-size: 0.85rem;
        opacity: 0.9;
    }

    .card-compacto {
        background: white;
        border-radius: 12px;
        box-shadow: 0 4px 15px rgba(0,0,0,0.08);
        border: none;
        margin-bottom: 1rem;
        overflow: hidden;
        position: relative; /* Para o ::before funcionar */
    }

        .card-compacto::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 3px;
            background: linear-gradient(135deg, var(--cor-primaria), var(--cor-secundaria));
        }

    .sidebar-compacto {
        background: white;
        border-radius: 12px;
        padding: 1rem;
        text-align: center;
        box-shadow: 0 4px 15px rgba(0,0,0,0.08);
        border-top: 3px solid var(--cor-amor);
        margin-bottom: 1rem;
    }

    .avatar-icon-compacto {
        font-size: 2.5rem;
        color: var(--cor-primaria);
        margin-bottom: 0.5rem;
    }

    .user-name-compacto {
        font-size: 1.1rem;
        font-weight: 600;
        color: #2c3e50;
        margin-bottom: 0.25rem;
    }

    .user-email-compacto {
        color: #6c757d;
        font-size: 0.8rem;
        margin-bottom: 1rem;
    }

    .btn-compacto {
        border-radius: 8px;
        padding: 0.4rem 0.8rem;
        font-size: 0.8rem;
        font-weight: 500;
        margin-bottom: 0.4rem;
        border: none;
        text-align: center;
        display: block; /* Para d-grid funcionar bem */
    }

    .btn-primary-compacto {
        background: var(--cor-primaria);
        color: white;
    }

    .btn-info-compacto {
        background: var(--cor-secundaria);
        color: white;
    }

    .btn-danger-compacto {
        background: transparent;
        border: 1px solid var(--cor-amor);
        color: var(--cor-amor);
    }

    .card-header-compacto {
        background: linear-gradient(135deg, var(--cor-primaria), var(--cor-secundaria));
        color: white;
        padding: 0.75rem 1rem;
        font-size: 0.95rem;
        font-weight: 600;
        display: flex;
        justify-content: space-between;
        align-items: center;
    }

    .card-header-info-compacto {
        background: linear-gradient(135deg, var(--cor-secundaria), var(--cor-accent));
    }

    .card-header-warning-compacto {
        background: linear-gradient(135deg, var(--cor-accent), #FF8A65);
        color: #2c3e50;
    }

    .card-body-compacto {
        padding: 1rem;
    }

    .info-row {
        margin-bottom: 0.75rem;
    }

    .info-label-compacto {
        font-weight: 500;
        color: #2c3e50;
        font-size: 0.8rem;
        margin-bottom: 0.2rem;
        display: flex;
        align-items: center;
    }

        .info-label-compacto i {
            color: var(--cor-primaria);
            margin-right: 0.3rem;
            width: 12px;
            font-size: 0.7rem;
        }

    .info-value-compacto {
        color: #6c757d;
        font-size: 0.85rem;
        padding-bottom: 0.5rem;
        border-bottom: 1px solid #f0f0f0;
    }

        .info-value-compacto:last-child {
            border-bottom: none;
            padding-bottom: 0;
        }

        .info-value-compacto a {
            color: var(--cor-primaria);
            text-decoration: none;
        }

    .child-card-compacto {
        background: #f8f9fa;
        border-radius: 8px;
        padding: 0.75rem;
        border-left: 3px solid var(--cor-accent);
        margin-bottom: 0.75rem;
    }

    .child-name-compacto {
        color: var(--cor-primaria);
        font-weight: 600;
        font-size: 0.9rem;
        margin-bottom: 0.5rem;
        display: flex;
        align-items: center;
    }

        .child-name-compacto i {
            margin-right: 0.3rem;
            color: var(--cor-amor);
            font-size: 0.8rem;
        }

    .child-info-compacto {
        font-size: 0.75rem;
        color: #6c757d;
        line-height: 1.4;
    }

        .child-info-compacto div {
            margin-bottom: 0.2rem;
        }

        .child-info-compacto strong {
            color: #2c3e50;
        }

    .badge-compacto {
        background: var(--cor-secundaria);
        color: white;
        border-radius: 10px;
        padding: 0.2rem 0.5rem;
        font-size: 0.7rem;
        font-weight: 500;
    }

    .contact-info-compacto {
        background: #e3f2fd;
        border-radius: 8px;
        padding: 0.75rem;
        border-left: 3px solid var(--cor-primaria);
        font-size: 0.8rem;
    }

    .empty-state-compacto {
        text-align: center;
        padding: 1.5rem;
        color: #6c757d;
    }

        .empty-state-compacto i {
            font-size: 2rem;
            margin-bottom: 0.5rem;
            color: var(--cor-accent);
            opacity: 0.7;
        }

    .text-center-btn {
        text-align: center;
        margin-top: 0.75rem;
    }

    /* Responsive */
    @@media (max-width: 768px) {
        .container-compacto

    {
        margin: 0.5rem auto;
    }

    .sidebar-compacto {
        margin-bottom: 1rem;
    }

    .avatar-icon-compacto {
        font-size: 2rem;
    }

    .header-title {
        font-size: 1.1rem;
    }

    }
</style>

<link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet">

<div class="container-compacto">
    <div class="header-compacto">
        <h1 class="header-title">
            <i class="fas fa-heart me-2"></i> Minha Família Espiritual
        </h1>
        <p class="header-subtitle mb-0">Casa Espírita Trabalhadores de Jesus</p>
    </div>

    @if (TempData["Sucesso"] != null)
    {
        <div class="alert alert-success alert-dismissible fade show" role="alert">
            <i class="fas fa-heart me-2"></i> @TempData["Sucesso"]
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
    }

    <div class="row">
        <div class="col-lg-3 col-md-4">
            <div class="sidebar-compacto">
                <i class="fas fa-user-circle avatar-icon-compacto"></i>
                <h5 class="user-name-compacto">@Model.Nome</h5>
                <p class="user-email-compacto">@Model.Email</p>

                <div class="d-grid gap-1">
                    <a href="/Perfil/Editar" class="btn btn-compacto btn-primary-compacto">
                        <i class="fas fa-edit me-1"></i> Editar
                    </a>
                    <a href="/Perfil/MinhasCriancas" class="btn btn-compacto btn-info-compacto">
                        <i class="fas fa-child me-1"></i> Crianças
                    </a>
                    @* O formulário de Logout será submetido pelo JS após a confirmação no Modal *@
                    <form asp-action="Logout" asp-controller="Auth" method="post" class="d-inline" id="logoutForm">
                        <button type="button" class="btn btn-compacto btn-danger-compacto w-100" id="logoutBtn" data-bs-toggle="modal" data-bs-target="#logoutConfirmModal">
                            <i class="fas fa-sign-out-alt me-1"></i> Sair
                        </button>
                    </form>
                </div>
            </div>
        </div>

        <div class="col-lg-9 col-md-8">
            <div class="card-compacto">
                <div class="card-header-compacto">
                    <i class="fas fa-user me-2"></i> Meus Dados
                </div>
                <div class="card-body-compacto">
                    <div class="row">
                        <div class="col-md-6">
                            <div class="info-row">
                                <div class="info-label-compacto">
                                    <i class="fas fa-user"></i> Nome
                                </div>
                                <div class="info-value-compacto">@Model.Nome</div>
                            </div>

                            <div class="info-row">
                                <div class="info-label-compacto">
                                    <i class="fas fa-id-card"></i> CPF
                                </div>
                                <div class="info-value-compacto">
                                    @if (!string.IsNullOrEmpty(Model.Cpf) && Model.Cpf.Length == 11)
                                    {
                                        @($"{Model.Cpf.Substring(0, 3)}.{Model.Cpf.Substring(3, 3)}.{Model.Cpf.Substring(6, 3)}-{Model.Cpf.Substring(9, 2)}")
                                    }
                                    else
                                    {
                                        @Model.Cpf
                                    }
                                </div>
                            </div>

                            <div class="info-row">
                                <div class="info-label-compacto">
                                    <i class="fas fa-phone"></i> Telefone
                                </div>
                                <div class="info-value-compacto">
                                    @if (!string.IsNullOrEmpty(Model.Telefone) && Model.Telefone.Length == 11)
                                    {
                                        <a href="tel:@Model.Telefone">
                                            @($"({Model.Telefone.Substring(0, 2)}) {Model.Telefone.Substring(2, 5)}-{Model.Telefone.Substring(7, 4)}")
                                        </a>
                                    }
                                    else
                                    {
                                        @Model.Telefone
                                    }
                                </div>
                            </div>
                        </div>

                        <div class="col-md-6">
                            <div class="info-row">
                                <div class="info-label-compacto">
                                    <i class="fas fa-envelope"></i> Email
                                </div>
                                <div class="info-value-compacto">
                                    <a href="mailto:@Model.Email">@Model.Email</a>
                                </div>
                            </div>

                            <div class="info-row">
                                <div class="info-label-compacto">
                                    <i class="fas fa-map-marker-alt"></i> Endereço
                                </div>
                                <div class="info-value-compacto">@Model.Endereco</div>
                            </div>

                            @if (Model.Ativo)
                            {
                                <div class="info-row">
                                    <div class="info-label-compacto">
                                        <i class="fas fa-heart"></i> Status
                                    </div>
                                    <div class="info-value-compacto">
                                        <span class="badge-compacto">
                                            <i class="fas fa-check-circle me-1"></i> Família Ativa
                                        </span>
                                    </div>
                                </div>
                            }
                        </div>
                    </div>
                </div>
            </div>

            <div class="card-compacto">
                <div class="card-header-compacto card-header-info-compacto">
                    <span>
                        <i class="fas fa-child me-2"></i> Nossas Crianças
                    </span>
                    <span class="badge bg-light text-dark ms-2">@(Model.Criancas?.Count ?? 0)</span>
                </div>
                <div class="card-body-compacto">
                    @if (Model.Criancas != null && Model.Criancas.Any())
                    {
                        <div class="row">
                            @foreach (var crianca in Model.Criancas)
                            {
                                <div class="col-lg-6">
                                    <div class="child-card-compacto">
                                        <h6 class="child-name-compacto">
                                            <i class="fas fa-child"></i> @crianca.Nome
                                        </h6>
                                        <div class="child-info-compacto">
                                            <div>
                                                <strong>CPF:</strong>
                                                @if (!string.IsNullOrEmpty(crianca.Cpf) && crianca.Cpf.Length == 11)
                                                {
                                                    @($"{crianca.Cpf.Substring(0, 3)}.{crianca.Cpf.Substring(3, 3)}.{crianca.Cpf.Substring(6, 3)}-{crianca.Cpf.Substring(9, 2)}")
                                                }
                                                else
                                                {
                                                    @crianca.Cpf
                                                }
                                            </div>
                                            <div><strong>Nascimento:</strong> @crianca.DataNascimento.ToString("dd/MM/yyyy")</div>
                                            <div>
                                                <strong>Idade:</strong>
                                                @{
                                                    var idade = DateTime.Now.Year - crianca.DataNascimento.Year;
                                                    if (DateTime.Now.DayOfYear < crianca.DataNascimento.DayOfYear) idade--;
                                                }
                                                @idade anos
                                            </div>
                                            <div><strong>Parentesco:</strong> @crianca.Parentesco</div>
                                        </div>
                                    </div>
                                </div>
                            }
                        </div>
                        <div class="text-center-btn">
                            <a href="/Perfil/MinhasCriancas" class="btn btn-compacto btn-primary-compacto">
                                <i class="fas fa-heart me-1"></i> Cuidar das Crianças
                            </a>
                        </div>
                    }
                    else
                    {
                        <div class="empty-state-compacto">
                            <i class="fas fa-child"></i>
                            <p class="mb-2">Ainda não há crianças cadastradas.</p>
                            <a href="/Perfil/Editar" class="btn btn-compacto btn-primary-compacto">
                                <i class="fas fa-plus me-1"></i> Cadastrar Criança
                            </a>
                        </div>
                    }
                </div>
            </div>

            <div class="card-compacto">
                <div class="card-header-compacto card-header-warning-compacto">
                    <i class="fas fa-calendar-alt me-2"></i> Minha Agenda
                </div>
                <div class="card-body-compacto">
                    <div class="empty-state-compacto">
                        <i class="fas fa-calendar-plus"></i>
                        <p class="mb-2">Agende consultas e cuidados para sua família.</p>
                       <div class="d-grid gap-1">
                            <a asp-action="Index" asp-controller="Agendamento" class="btn btn-compacto btn-primary-compacto">
                                <i class="fas fa-plus me-1"></i> Agendar Consulta
                            </a>

                            <a asp-action="MinhaAgenda" asp-controller="Agendamento" class="btn btn-compacto btn-info-compacto">
                                <i class="fas fa-calendar me-1"></i> Ver Agenda Completa
                            </a>
                        </div>

                        <div class="contact-info-compacto mt-3">
                            <div class="text-center">
                                <div class="info-label-compacto justify-content-center">
                                    <i class="fas fa-phone"></i> Contato para agendamentos
                                </div>
                                <div class="info-value-compacto">
                                    <a href="tel:1194155-6472">(11) 94155-6472</a>
                                </div>
                                <small style="font-size: 0.7rem; color: #6c757d;">
                                    <i class="fas fa-heart text-danger me-1"></i>
                                    "Cuidamos de você com amor"
                                </small>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

@* ========================================================= *@
@* 1. MODAL DE SUCESSO DE AGENDAMENTO (POP-UP) *@
@* ========================================================= *@
<div class="modal fade" id="successModal" tabindex="-1" aria-labelledby="successModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content" style="border-radius: 15px; border: 3px solid var(--cor-secundaria);">
            
            <div class="modal-header" style="background-color: var(--cor-secundaria); color: white; border-radius: 12px 12px 0 0;">
                <h5 class="modal-title" id="successModalLabel">
                    @(TempData["SuccessMessageTitle"] ?? "Sucesso!")
                </h5>
                <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal" aria-label="Fechar"></button>
            </div>
            
            <div class="modal-body text-center">
                <h3 style="color: var(--cor-secundaria); margin-bottom: 20px;">✅ Agendamento Realizado!</h3>
                <p style="font-size: 1.1rem;">
                    @(TempData["SuccessMessageBody"] ?? "Nos vemos em breve! Sua consulta foi confirmada.")
                </p>
            </div>
            
            <div class="modal-footer justify-content-center">
                <button type="button" class="btn btn-success" data-bs-dismiss="modal" style="background-color: var(--cor-secundaria); border-color: var(--cor-secundaria);">Fechar</button>
            </div>
        </div>
    </div>
</div>

@* ========================================================= *@
@* 2. MODAL DE CONFIRMAÇÃO DE SAÍDA (LOGOUT) *@
@* ========================================================= *@
<div class="modal fade" id="logoutConfirmModal" tabindex="-1" aria-labelledby="logoutConfirmModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered modal-sm">
        <div class="modal-content" style="border-radius: 15px; border: 3px solid var(--cor-amor);">
            
            <div class="modal-header" style="background-color: var(--cor-amor); color: white; border-radius: 12px 12px 0 0;">
                <h5 class="modal-title" id="logoutConfirmModalLabel">
                    <i class="fas fa-sign-out-alt me-1"></i> Confirmar Saída
                </h5>
                <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal" aria-label="Fechar"></button>
            </div>
            
            <div class="modal-body text-center">
                @* Ícone de coração partido removido, substituído por um ícone de sign-out para manter a consistência com o título *@
                <i class="fas fa-sign-out-alt fa-3x" style="color: var(--cor-amor); margin-bottom: 15px;"></i>
                <p class="mb-0">
                    Tem certeza que deseja sair da sua conta?
                </p>
                <small class="text-muted">Você precisará fazer login novamente para acessar seus dados.</small>
            </div>
            
            <div class="modal-footer justify-content-center border-0">
                <button type="button" class="btn btn-outline-secondary btn-compacto" data-bs-dismiss="modal">
                    Ficar
                </button>
                <button type="button" class="btn btn-compacto" id="confirmLogoutBtn" style="background-color: var(--cor-amor); color: white;">
                    Sim, Sair
                </button>
            </div>
        </div>
    </div>
</div>


@* ========================================================= *@
@* 3. SCRIPTS PARA EXIBIR O MODAL E OUTRAS FUNÇÕES *@
@* ========================================================= *@
@section Scripts {
    <script>
        
        // ======================================================
        // LÓGICA DE EXIBIÇÃO DO MODAL DE CONFIRMAÇÃO DE SAÍDA
        // ======================================================
        document.getElementById('confirmLogoutBtn').addEventListener('click', function(e) {
            // Submete o formulário de logout após a confirmação no modal
            document.getElementById('logoutForm').submit();
        });


        // ======================================================
        // LÓGICA DE EXIBIÇÃO DO MODAL DE SUCESSO (CHAVE)
        // O valor do TempData é injetado aqui pelo Razor
        // ======================================================
        const agendamentoSucesso = '@TempData["AgendamentoSucesso"]' === 'True';

        if (agendamentoSucesso) {
            // Verifica se o Bootstrap JS está carregado e mostra o modal
            if (typeof bootstrap !== 'undefined') {
                const successModalElement = document.getElementById('successModal');
                const successModal = new bootstrap.Modal(successModalElement);
                successModal.show();
            } else {
                console.error("Bootstrap JS não carregado. Não foi possível exibir o modal de sucesso.");
            }
        }
    </script>
}
